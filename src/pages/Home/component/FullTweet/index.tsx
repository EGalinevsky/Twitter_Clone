import React from 'react'
import { TweetComponent } from '../../../../components/Tweet'

export const FullTweet: React.FC = (): React.ReactElement => {

    return (
        <div>
            ndbn
            {/* <TweetComponent key={tweet._id} classes={classes} {...tweet} /> */}
        </div>
    )
}
